# GitHub Pages

Publishing source is configured as **GitHub Actions**.

The deployment workflow in `.github/workflows/deploy-pages.yml` builds the static `_site` artifact and deploys it through GitHub Pages.

Expected project URL:

`https://nestlir.github.io/serendipity-archive-v0.1.0/`
